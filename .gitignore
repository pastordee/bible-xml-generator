# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# API Keys (if stored in separate file)
*.env
.env
config.py

# Generated XML files (these are large and can be regenerated)
esv/*.xml
kjv/*.xml
niv/*.xml
nkjv/*.xml
amp/*.xml
msg/*.xml
esv_new/*.xml

# Keep directory structure but ignore XML files
!.gitkeep

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Books directory (if generated)
books/

# State files
state.json
xml_esv/*.xml
raw/cross_refs_esv/
xml_twi/*.xml
xml_twiasante/*.xml

# Dictionary XML: downloaded source plus the cleaned output, both regenerated
# by make_eastons_dictionary.py and shipped as a zip in readyForServer/.
dictionaries/*.xml

# Screenshots pasted in during review are not repo content.
Screenshot*.png
